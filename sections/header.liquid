<!-- Premium Glassmorphic Header Component -->
<style>
  /* Collapsing Shopify Section containers to start content at y=0 */
  .section-header,
  .shopify-section-group-header-group {
    position: absolute !important;
    top: 0 !important;
    left: 0 !important;
    height: 0 !important;
    width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
    overflow: visible !important;
    pointer-events: none !important;
  }

  /* Custom Glassmorphic Header CSS */
  .custom-glass-header-wrapper {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    width: 100% !important;
    z-index: 1000 !important;
    padding: 0 !important;
    box-sizing: border-box !important;
    pointer-events: none !important;
    background: transparent !important;
    transition: padding 0.5s cubic-bezier(0.16, 1, 0.3, 1) !important;
    --glass-opacity: {{ section.settings.glass_opacity | divided_by: 100.0 }};
    --glass-radius: {{ section.settings.glass_radius }}px;
  }

  .custom-glass-header {
    pointer-events: auto !important;
    display: grid !important;
    grid-template-columns: 1fr auto 1fr !important;
    align-items: center !important;
    width: 100% !important;
    height: 72px !important;
    padding: 0 32px !important;
    box-sizing: border-box !important;
    background: rgba(var(--color-background), var(--glass-opacity)) !important;
    backdrop-filter: blur(20px) saturate(190%) !important;
    -webkit-backdrop-filter: blur(20px) saturate(190%) !important;
    border-bottom: 1px solid rgba(var(--color-foreground), 0.08) !important;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.02) !important;
    transition: border-radius 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                border-color 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                margin 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                height 0.5s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  /* Scrolled Floating State */
  .custom-glass-header-wrapper.is-scrolled {
    padding: 16px 24px 0 24px !important;
  }

  .custom-glass-header-wrapper.is-scrolled .custom-glass-header {
    border-radius: var(--glass-radius) !important;
    border: 1px solid rgba(var(--color-foreground), 0.15) !important;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08) !important;
    height: 64px !important;
  }

  /* Layout alignment */
  .custom-header-left {
    display: flex !important;
    justify-content: flex-start !important;
    align-items: center !important;
  }

  .custom-header-center {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
  }

  .custom-header-right {
    display: flex !important;
    justify-content: flex-end !important;
    align-items: center !important;
    gap: 20px !important;
  }

  /* Hamburger button */
  .header-hamburger-btn {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    width: 44px !important;
    height: 44px !important;
    padding: 0 !important;
    margin: 0 !important;
    line-height: 0 !important;
    border-radius: 50% !important;
    background: rgba(var(--color-foreground), 0.06) !important;
    color: rgb(var(--color-foreground)) !important;
    border: 1px solid rgba(var(--color-foreground), 0.08) !important;
    cursor: pointer !important;
    outline: none !important;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .header-hamburger-btn:hover {
    background: rgba(var(--color-foreground), 0.12) !important;
    border-color: rgba(var(--color-foreground), 0.15) !important;
    transform: scale(1.05) !important;
  }

  .header-hamburger-btn:active {
    transform: scale(0.95) !important;
  }

  /* Hamburger Icon Line Morphing to Cross (X) */
  .hamburger-icon-svg {
    display: block !important;
    overflow: visible !important;
    margin: 0 auto !important;
  }

  .hamburger-line {
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1) !important;
    transform-box: view-box !important;
    transform-origin: 9px 9px !important;
  }

  /* Default closed state (=): Translate line 1 UP by -4px and line 2 DOWN by +4px */
  .line-1 {
    transform: translateY(-4px) rotate(0deg) !important;
  }

  .line-2 {
    transform: translateY(4px) rotate(0deg) !important;
  }

  /* Open state (X): Rotate in-place around center (9, 9) with zero translateY displacement */
  .mega-menu-open .line-1 {
    transform: translateY(0) rotate(45deg) !important;
  }

  .mega-menu-open .line-2 {
    transform: translateY(0) rotate(-45deg) !important;
  }

  /* Logo image & text styling */
  .header-logo-link {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    text-decoration: none !important;
    color: rgb(var(--color-foreground)) !important;
  }

  .header-logo-img {
    display: block !important;
    max-height: 40px !important;
    width: auto !important;
    object-fit: contain !important;
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .header-logo-link:hover .header-logo-img {
    transform: scale(1.03) !important;
  }

  .header-logo-text {
    font-family: var(--font-heading-family) !important;
    font-weight: var(--font-heading-weight) !important;
    font-size: 2.2rem !important;
    letter-spacing: 0.1rem !important;
    text-transform: uppercase !important;
    white-space: nowrap !important;
  }

  /* Plain icon buttons (Search, Profile) */
  .header-icon-btn {
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    color: rgb(var(--color-foreground)) !important;
    text-decoration: none !important;
    width: 36px !important;
    height: 36px !important;
    border-radius: 50% !important;
    background: transparent !important;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .header-icon-btn:hover {
    background: rgba(var(--color-foreground), 0.06) !important;
    transform: scale(1.1) !important;
  }

  .header-icon-btn:active {
    transform: scale(0.92) !important;
  }

  /* Cart dynamic pill button */
  .header-cart-pill {
    display: inline-flex !important;
    align-items: center !important;
    gap: 8px !important;
    padding: 10px 18px !important;
    border-radius: 9999px !important;
    background: rgba(var(--color-foreground), 0.06) !important;
    color: rgb(var(--color-foreground)) !important;
    text-decoration: none !important;
    font-size: 1.4rem !important;
    font-weight: 500 !important;
    border: 1px solid rgba(var(--color-foreground), 0.08) !important;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .header-cart-pill:hover {
    background: rgba(var(--color-foreground), 0.12) !important;
    border-color: rgba(var(--color-foreground), 0.15) !important;
    transform: scale(1.05) !important;
  }

  .header-cart-pill:active {
    transform: scale(0.96) !important;
  }

  .header-cart-pill svg {
    flex-shrink: 0 !important;
  }

  /* Visibility Helpers */
  .hide-on-desktop {
    display: none !important;
  }
  .hide-on-mobile {
    display: inline-flex !important;
  }

  /* Mobile/Tablet responsive fixes */
  @media screen and (max-width: 989px) {
    .hide-on-desktop {
      display: inline-flex !important;
    }
    .hide-on-mobile {
      display: none !important;
    }

    .custom-glass-header {
      padding: 0 16px !important;
      height: 64px !important;
    }
    
    .custom-glass-header-wrapper.is-scrolled {
      padding: 10px 12px 0 12px !important;
    }
    
    .custom-glass-header-wrapper.is-scrolled .custom-glass-header {
      height: 56px !important;
    }
    
    /* LEFT: Search aligned next to hamburger button */
    .custom-header-left {
      gap: 12px !important;
    }

    /* Remove round bubble style on hamburger button */
    .header-hamburger-btn {
      background: transparent !important;
      border: none !important;
      width: 24px !important;
      height: 24px !important;
      border-radius: 0 !important;
      padding: 0 !important;
      color: rgb(var(--color-foreground)) !important;
      box-shadow: none !important;
    }
    .header-hamburger-btn:hover {
      background: transparent !important;
      transform: none !important;
    }
    .header-hamburger-btn:active {
      transform: scale(0.92) !important;
    }

    /* RIGHT: gap adjustment */
    .custom-header-right {
      gap: 12px !important;
    }

    .header-icon-btn {
      width: 32px !important;
      height: 32px !important;
    }
    
    /* Make Cart Pill look like a flat icon button (no pill wrapper/text) */
    .header-cart-pill {
      background: transparent !important;
      border: none !important;
      border-radius: 50% !important;
      padding: 0 !important;
      width: 32px !important;
      height: 32px !important;
      justify-content: center !important;
      gap: 0 !important;
      box-shadow: none !important;
    }
    .header-cart-pill:hover {
      background: rgba(var(--color-foreground), 0.06) !important;
      transform: scale(1.1) !important;
    }
    .header-cart-pill:active {
      transform: scale(0.92) !important;
    }
    .header-cart-pill .header-cart-text {
      display: none !important;
    }

    /* Scaling Logo and Text */
    .header-logo-text {
      font-size: 1.8rem !important;
    }
    .header-logo-img {
      max-width: 120px !important;
    }
  }

  /* ==========================================================================
     MEGA MENU DROPDOWN PANEL STYLES
     ========================================================================== */
  .custom-mega-menu-panel {
    pointer-events: auto !important;
    width: 100% !important;
    background: rgba(var(--color-background), var(--glass-opacity)) !important;
    backdrop-filter: blur(20px) saturate(190%) !important;
    -webkit-backdrop-filter: blur(20px) saturate(190%) !important;
    border: 1px solid rgba(var(--color-foreground), 0.12) !important;
    border-radius: var(--glass-radius) !important;
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.08) !important;
    box-sizing: border-box !important;
    overflow: hidden !important;
    opacity: 0 !important;
    visibility: hidden !important;
    transform: translateY(-10px) scale(0.99) !important;
    max-height: 0 !important;
    margin-top: 8px !important;
    transition: opacity 0.35s cubic-bezier(0.16, 1, 0.3, 1),
                transform 0.35s cubic-bezier(0.16, 1, 0.3, 1),
                visibility 0.35s cubic-bezier(0.16, 1, 0.3, 1),
                max-height 0.4s cubic-bezier(0.16, 1, 0.3, 1),
                margin 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                border-radius 0.5s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  /* Consistent 8px gap and rounded corners in both scrolled and non-scrolled states */
  .custom-mega-menu-panel {
    margin-top: 8px !important;
    border-radius: var(--glass-radius) !important;
  }

  /* Open Active State */
  .custom-glass-header-wrapper.mega-menu-open .custom-mega-menu-panel {
    opacity: 1 !important;
    visibility: visible !important;
    transform: translateY(0) scale(1) !important;
    max-height: 85vh !important;
    overflow-y: auto !important;
  }

  /* Inner Structure & Desktop 4-Column Grid */
  .custom-mega-menu-inner {
    padding: 40px 48px !important;
    box-sizing: border-box !important;
    height: auto !important;
    width: 100% !important;
  }

  .custom-mega-menu-grid {
    display: grid !important;
    grid-template-columns: repeat(4, 1fr) !important;
    gap: 40px 36px !important;
    width: 100% !important;
    box-sizing: border-box !important;
  }

  /* Main Category Column (A) */
  .mega-menu-col {
    display: flex !important;
    flex-direction: column !important;
    gap: 16px !important;
    min-width: 0 !important;
  }

  .mega-menu-main-title {
    font-family: var(--font-heading-family), sans-serif !important;
    font-size: 14px !important;
    font-weight: 700 !important;
    letter-spacing: 0.08em !important;
    text-transform: uppercase !important;
    color: rgb(var(--color-foreground)) !important;
    margin: 0 0 4px 0 !important;
    padding-bottom: 8px !important;
    border-bottom: 1.5px solid rgba(var(--color-foreground), 0.12) !important;
  }

  .mega-menu-main-title a {
    color: inherit !important;
    text-decoration: none !important;
    transition: opacity 0.2s ease !important;
  }

  .mega-menu-main-title a:hover {
    opacity: 0.65 !important;
  }

  /* Sub Categories Container (B) */
  .mega-menu-subcategories {
    display: flex !important;
    flex-direction: column !important;
    gap: 20px !important;
  }

  .mega-menu-sub-group {
    display: flex !important;
    flex-direction: column !important;
    gap: 8px !important;
  }

  .mega-menu-sub-title {
    font-family: var(--font-body-family), sans-serif !important;
    font-size: 12px !important;
    font-weight: 600 !important;
    letter-spacing: 0.05em !important;
    text-transform: uppercase !important;
    color: rgba(var(--color-foreground), 0.85) !important;
    margin: 0 !important;
  }

  .mega-menu-sub-title a {
    color: inherit !important;
    text-decoration: none !important;
    transition: color 0.2s ease !important;
  }

  .mega-menu-sub-title a:hover {
    color: rgb(var(--color-foreground)) !important;
  }

  /* Collection Name Links List (C) */
  .mega-menu-collection-list {
    list-style: none !important;
    margin: 0 !important;
    padding: 0 0 0 10px !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 6px !important;
    border-left: 1.5px solid rgba(var(--color-foreground), 0.1) !important;
  }

  .mega-menu-collection-item {
    margin: 0 !important;
  }

  .mega-menu-collection-link {
    font-family: var(--font-body-family), sans-serif !important;
    font-size: 11px !important;
    font-weight: 400 !important;
    color: rgba(var(--color-foreground), 0.65) !important;
    text-decoration: none !important;
    transition: color 0.2s ease, transform 0.2s ease !important;
    display: inline-block !important;
  }

  /* Sub Category Header & Toggle Chevron Button */
  .mega-menu-sub-header {
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    gap: 8px !important;
    width: 100% !important;
  }

  .mega-menu-toggle-btn {
    display: none !important; /* Hidden on desktop view */
    align-items: center !important;
    justify-content: center !important;
    width: 28px !important;
    height: 28px !important;
    padding: 0 !important;
    margin: 0 !important;
    background: transparent !important;
    border: none !important;
    color: rgba(var(--color-foreground), 0.65) !important;
    cursor: pointer !important;
    outline: none !important;
  }

  .chevron-icon {
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
    display: block !important;
  }

  /* Desktop View Default: Collection List is always expanded */
  @media screen and (min-width: 750px) {
    .mega-menu-collection-list {
      display: flex !important;
      opacity: 1 !important;
      visibility: visible !important;
      max-height: none !important;
      margin-top: 6px !important;
    }
  }

  /* Mobile Layout Adjustment & Accordion (max-width: 749px) */
  @media screen and (max-width: 749px) {
    .custom-mega-menu-inner {
      padding: 24px 20px !important;
    }

    .custom-mega-menu-grid {
      grid-template-columns: 1fr !important;
      gap: 28px !important;
    }

    .mega-menu-toggle-btn {
      display: inline-flex !important; /* Visible on mobile */
    }

    .mega-menu-sub-header {
      cursor: pointer !important;
    }

    /* Collapsed collection list on mobile */
    .mega-menu-collection-list {
      max-height: 0 !important;
      opacity: 0 !important;
      visibility: hidden !important;
      overflow: hidden !important;
      margin-top: 0 !important;
      padding-top: 0 !important;
      padding-bottom: 0 !important;
      transition: max-height 0.35s cubic-bezier(0.16, 1, 0.3, 1),
                  opacity 0.25s ease,
                  visibility 0.25s ease,
                  margin 0.35s ease,
                  padding 0.35s ease !important;
    }

    /* Expanded collection list on mobile when .is-open */
    .mega-menu-sub-group.is-open .mega-menu-collection-list {
      max-height: 500px !important;
      opacity: 1 !important;
      visibility: visible !important;
      margin-top: 8px !important;
      padding-top: 4px !important;
    }

    /* Rotate Chevron Down Arrow to Up Arrow when .is-open */
    .mega-menu-sub-group.is-open .chevron-icon {
      transform: rotate(180deg) !important;
      color: rgb(var(--color-foreground)) !important;
    }
  }
</style>

<div 
  class="custom-glass-header-wrapper color-{{ section.settings.color_scheme }}" 
  id="custom-glass-header-wrapper" 
  style="--glass-opacity: {{ section.settings.glass_opacity | divided_by: 100.0 }}; --glass-radius: {{ section.settings.glass_radius }}px;"
>
  <header class="custom-glass-header">
    
    <!-- LEFT: Hamburger Menu and Search Icon (Mobile) -->
    <div class="custom-header-left">
      <button class="header-hamburger-btn" aria-label="Menu" id="header-hamburger-menu-btn">
        <svg class="hamburger-icon-svg" width="18" height="18" viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path class="hamburger-line line-1" d="M2 9H16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
          <path class="hamburger-line line-2" d="M2 9H16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
        </svg>
      </button>
      
      <!-- Search Button (Visible on Mobile only) -->
      <a href="{{ routes.search_url }}" class="header-icon-btn hide-on-desktop" aria-label="Search">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </a>
    </div>

    <!-- MIDDLE: Logo -->
    <div class="custom-header-center">
      <a href="{{ routes.root_url }}" class="header-logo-link">
        {%- if settings.logo != blank -%}
          {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
          {%- assign logo_height = section.settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
          {{
            settings.logo
            | image_url: width: 600
            | image_tag:
              class: 'header-logo-img',
              height: logo_height,
              width: section.settings.logo_width,
              alt: logo_alt,
              preload: true
          }}
        {%- else -%}
          <span class="header-logo-text">{{ shop.name }}</span>
        {%- endif -%}
      </a>
    </div>

    <!-- RIGHT: Search, Profile, Cart -->
    <div class="custom-header-right">
      <!-- Search Icon Button (Visible on Desktop only) -->
      <a href="{{ routes.search_url }}" class="header-icon-btn hide-on-mobile" aria-label="Search">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </a>

      <!-- Profile Icon Button -->
      {%- if shop.customer_accounts_enabled -%}
        <a href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}" class="header-icon-btn" aria-label="Account">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
        </a>
      {%- endif -%}

      <!-- Cart Dynamic Pill Badge -->
      <a href="{{ routes.cart_url }}" class="header-cart-pill" id="cart-icon-bubble">
        <svg class="icon icon-cart" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <path d="M16 10a4 4 0 0 1-8 0"></path>
        </svg>
        <span class="header-cart-text">Cart (<span id="cart-count-value">{{ cart.item_count }}</span>)</span>
      </a>
    </div>
    
  </header>

  <!-- Mega Menu Dropdown Panel (Desktop 4-Column Structure) -->
  <div class="custom-mega-menu-panel" id="custom-mega-menu-panel">
    <div class="custom-mega-menu-inner" id="custom-mega-menu-inner">
      {%- liquid
        assign mega_menu = section.settings.menu | default: linklists['main-menu']
        assign main_category_blocks = section.blocks | where: 'type', 'main_category'
      -%}

      <div class="custom-mega-menu-grid">
        {%- if main_category_blocks.size > 0 -%}
          {% comment %} 1. Customizer Blocks Mode: Render section.blocks added directly in Theme Customizer {% endcomment %}
          {%- for block in main_category_blocks -%}
            <div class="mega-menu-col" {{ block.shopify_attributes }}>
              {%- if block.settings.title != blank -%}
                <h3 class="mega-menu-main-title">
                  <a href="{{ block.settings.url | default: '#' }}">{{ block.settings.title | escape }}</a>
                </h3>
              {%- endif -%}

              {%- assign block_menu = block.settings.menu -%}
              {%- if block_menu != blank and block_menu.links.size > 0 -%}
                <div class="mega-menu-subcategories">
                  {%- for sub_cat in block_menu.links -%}
                    <div class="mega-menu-sub-group">
                      <div class="mega-menu-sub-header">
                        <h4 class="mega-menu-sub-title">
                          <a href="{{ sub_cat.url }}">{{ sub_cat.title | escape }}</a>
                        </h4>
                        {%- if sub_cat.links.size > 0 -%}
                          <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle {{ sub_cat.title | escape }}" aria-expanded="false">
                            <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                              <polyline points="6 9 12 15 18 9"></polyline>
                            </svg>
                          </button>
                        {%- endif -%}
                      </div>

                      {%- if sub_cat.links.size > 0 -%}
                        <ul class="mega-menu-collection-list">
                          {%- for coll_item in sub_cat.links -%}
                            <li class="mega-menu-collection-item">
                              <a href="{{ coll_item.url }}" class="mega-menu-collection-link">
                                {{ coll_item.title | escape }}
                              </a>
                            </li>
                          {%- endfor -%}
                        </ul>
                      {%- endif -%}
                    </div>
                  {%- endfor -%}
                </div>
              {%- endif -%}
            </div>
          {%- endfor -%}

        {%- elsif mega_menu != blank and mega_menu.links.size > 0 -%}
          {% comment %} 2. Shopify Navigation Menu Mode: Render 3-level menu from Shopify Admin -> Navigation {% endcomment %}
          {%- for main_cat in mega_menu.links -%}
            <div class="mega-menu-col">
              <h3 class="mega-menu-main-title">
                <a href="{{ main_cat.url }}">{{ main_cat.title | escape }}</a>
              </h3>

              {%- if main_cat.links.size > 0 -%}
                <div class="mega-menu-subcategories">
                  {%- for sub_cat in main_cat.links -%}
                    <div class="mega-menu-sub-group">
                      <div class="mega-menu-sub-header">
                        <h4 class="mega-menu-sub-title">
                          <a href="{{ sub_cat.url }}">{{ sub_cat.title | escape }}</a>
                        </h4>
                        {%- if sub_cat.links.size > 0 -%}
                          <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle {{ sub_cat.title | escape }}" aria-expanded="false">
                            <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                              <polyline points="6 9 12 15 18 9"></polyline>
                            </svg>
                          </button>
                        {%- endif -%}
                      </div>

                      {%- if sub_cat.links.size > 0 -%}
                        <ul class="mega-menu-collection-list">
                          {%- for coll_item in sub_cat.links -%}
                            <li class="mega-menu-collection-item">
                              <a href="{{ coll_item.url }}" class="mega-menu-collection-link">
                                {{ coll_item.title | escape }}
                              </a>
                            </li>
                          {%- endfor -%}
                        </ul>
                      {%- endif -%}
                    </div>
                  {%- endfor -%}
                </div>
              {%- endif -%}
            </div>
          {%- endfor -%}

        {%- else -%}
          {% comment %} 3. Rich Default Demo Menu Structure (Top wear, Bottom wear, Outerwear, Accessories) {% endcomment %}
          <div class="mega-menu-col">
            <h3 class="mega-menu-main-title"><a href="/collections/all">TOP WEAR</a></h3>
            <div class="mega-menu-subcategories">
              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/t-shirts">Oversized T-Shirts</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Oversized T-Shirts" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Anime</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Minimalist Graphic</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Vintage Wash</a></li>
                </ul>
              </div>

              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/shirts">Shirts</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Shirts" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Resort Collar</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Oversized Linen</a></li>
                </ul>
              </div>
            </div>
          </div>

          <div class="mega-menu-col">
            <h3 class="mega-menu-main-title"><a href="/collections/all">BOTTOM WEAR</a></h3>
            <div class="mega-menu-subcategories">
              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/all">Denims</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Denims" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Barrel Denims</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Baggy Wide Fit</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Distressed Vintage</a></li>
                </ul>
              </div>

              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/all">Cargo Pants</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Cargo Pants" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Parachute Cargos</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Tactical Multi-pocket</a></li>
                </ul>
              </div>
            </div>
          </div>

          <div class="mega-menu-col">
            <h3 class="mega-menu-main-title"><a href="/collections/all">OUTERWEAR</a></h3>
            <div class="mega-menu-subcategories">
              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/all">Jackets & Hoodies</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Jackets & Hoodies" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Heavyweight Hoodies</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Bomber Jackets</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Fleece Zip-Ups</a></li>
                </ul>
              </div>
            </div>
          </div>

          <div class="mega-menu-col">
            <h3 class="mega-menu-main-title"><a href="/collections/all">ACCESSORIES</a></h3>
            <div class="mega-menu-subcategories">
              <div class="mega-menu-sub-group">
                <div class="mega-menu-sub-header">
                  <h4 class="mega-menu-sub-title"><a href="/collections/all">Headwear & Gear</a></h4>
                  <button type="button" class="mega-menu-toggle-btn" aria-label="Toggle Headwear & Gear" aria-expanded="false">
                    <svg class="chevron-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                  </button>
                </div>
                <ul class="mega-menu-collection-list">
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Embroidered Caps</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Knit Beanies</a></li>
                  <li class="mega-menu-collection-item"><a href="/collections/all" class="mega-menu-collection-link">Tote Bags</a></li>
                </ul>
              </div>
            </div>
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>




{% javascript %}
  document.addEventListener('DOMContentLoaded', () => {
    const headerWrapper = document.getElementById('custom-glass-header-wrapper');
    const hamburgerBtn = document.getElementById('header-hamburger-menu-btn');
    if (!headerWrapper) return;

    // Toggle Mega Menu on Hamburger Click
    if (hamburgerBtn) {
      hamburgerBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        headerWrapper.classList.toggle('mega-menu-open');
      });
    }

    // Close Mega Menu when clicking outside header wrapper
    document.addEventListener('click', (e) => {
      if (headerWrapper.classList.contains('mega-menu-open') && !headerWrapper.contains(e.target)) {
        headerWrapper.classList.remove('mega-menu-open');
      }
    });

    // Close Mega Menu on Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && headerWrapper.classList.contains('mega-menu-open')) {
        headerWrapper.classList.remove('mega-menu-open');
      }
    });

    const threshold = 10; // Scroll threshold in pixels

    function checkScroll() {
      const scrollY = window.pageYOffset || document.documentElement.scrollTop;
      if (scrollY > threshold) {
        headerWrapper.classList.add('is-scrolled');
      } else {
        headerWrapper.classList.remove('is-scrolled');
      }
    }

    // Run check on load in case the page is already scrolled
    checkScroll();

    let ticking = false;
    window.addEventListener('scroll', () => {
      if (!ticking) {
        window.requestAnimationFrame(() => {
          checkScroll();
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });

    // Mobile Sub-category Accordion Toggle (triggers on click of sub-category title or arrow)
    const subHeaders = document.querySelectorAll('.mega-menu-sub-header');
    subHeaders.forEach(header => {
      header.addEventListener('click', (e) => {
        // Only run accordion toggle on mobile screens (< 750px)
        if (window.innerWidth >= 750) return;

        e.preventDefault();
        e.stopPropagation();

        const currentSubGroup = header.closest('.mega-menu-sub-group');
        if (!currentSubGroup) return;

        const isAlreadyOpen = currentSubGroup.classList.contains('is-open');

        // Behavior 1: Close all other open sub-categories on mobile
        document.querySelectorAll('.mega-menu-sub-group.is-open').forEach(group => {
          group.classList.remove('is-open');
          const toggleBtn = group.querySelector('.mega-menu-toggle-btn');
          if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'false');
        });

        // Behavior 2: Toggle clicked sub-category (expand if closed, contract if already open)
        if (!isAlreadyOpen) {
          currentSubGroup.classList.add('is-open');
          const toggleBtn = currentSubGroup.querySelector('.mega-menu-toggle-btn');
          if (toggleBtn) toggleBtn.setAttribute('aria-expanded', 'true');
        }
      });
    });
  });
{% endjavascript %}

{% schema %}
{
  "name": "Custom Glass Header",
  "class": "section-header",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 250,
      "step": 10,
      "default": 150,
      "unit": "px",
      "label": "Logo width"
    },
    {
      "type": "range",
      "id": "glass_opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 80,
      "unit": "%",
      "label": "Glass Opacity"
    },
    {
      "type": "range",
      "id": "glass_radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "default": 16,
      "unit": "px",
      "label": "Scrolled Border Radius"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Mega Menu Navigation",
      "default": "main-menu",
      "info": "Select a 3-level menu (Main Category -> Sub Category -> Collection Name) created in Shopify Admin -> Navigation."
    }
  ],
  "blocks": [
    {
      "type": "main_category",
      "name": "Main Category Column",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Main Category Title",
          "default": "Top wear"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Main Category Link"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Sub Categories & Collections Menu",
          "info": "Nested menu containing Sub Categories (Level 1) and Collection Links (Level 2)."
        }
      ]
    }
  ]
}
{% endschema %}
